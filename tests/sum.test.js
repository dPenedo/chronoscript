import { sum, resta } from '../js/sum';

test('suma 1 y 2 y da 3', () => {
    expect(sum(1, 2)).toBe(3);
});

test('resta 4 y 2 y da 2', () => {
    expect(resta(4, 2)).toBe(2);
});

test('resta 4 y 2 y da 2', () => {
    expect(resta(4, 2)).toBe(2);
});
